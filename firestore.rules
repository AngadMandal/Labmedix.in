rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Master security and multi-device live data rules for LABMEDIX Cloud Health System
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
